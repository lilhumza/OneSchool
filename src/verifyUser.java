import java.io.File;
import java.io.IOException;
import java.util.Scanner;

//Checks to see if the user name and password match the account info if its correct you will log in if not and error will pop up
public class verifyUser {
    static File file;
    static String[] data;
    public static boolean verify(String username, String password) throws IOException {
    	return true;
    }
}
